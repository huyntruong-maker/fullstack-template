---
description: Write a short PRD/spec for a feature using the spec-driven-development skill.
argument-hint: "<feature or change to spec>"
---

Apply the `spec-driven-development` skill to: $ARGUMENTS

Produce `docs/specs/<feature>.md` with: Objective (who + why), numbered Functional Requirements (FR-001 ...), Non-Goals, Constraints, Open Questions & Assumptions, and Acceptance criteria per FR. Resolve blocking unknowns or record them as assumptions, then ask the user to confirm before planning.

Verification: the spec file exists with all sections, every FR is a uniquely numbered testable statement, and a non-goals section is present and non-empty.
