# API Reference — <PROJECT_NAME>

Owner agent: backend-lead. Update after every endpoint change.

## Conventions
Base URL, versioning (`/api/v1`), auth scheme, error format (RFC 7807 ProblemDetails).

## Endpoints
### <METHOD> /api/v1/<resource>
- Purpose:
- Request:
- Response (200 / errors):
- Auth: 
